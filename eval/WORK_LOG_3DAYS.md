# 작업 기록 — 최근 3일 (08-08 ~ 08-10)

> 브랜치 `feat/llm-classifier-chunking-fixes`, 커밋 30개. 각 항목은 "문제 → 원인 → 조치 → 실측 근거"
> 순으로 정리했으며, 근거는 커밋에 실제로 기록된 수치(회귀 게이트 결과)를 그대로 인용한다.
> 실측에 쓰인 기준 문서: `30327`(단체안심생활보험), `프리미엄2604`, `DB빅히트`(3문서 상시 게이트) +
> `in/` 20개 약관 배치.

---

## 1. 08-08 — 경계 검출 엔진 재설계 (하루 전체가 하나의 축)

이날의 핵심 문제의식: **특약 경계(어디서 한 특약이 끝나고 다음이 시작하는지)를 못 잡으면
그 뒤의 조 분할·청킹이 전부 틀어진다.** 그래서 경계 검출부터 다시 설계했다.

| 커밋 | 문제 | 조치 | 근거(실측) |
|---|---|---|---|
| `72ab4e6` feat(boundaries) | 감긴 제목(2~3줄), 문장 꼬리 분리, 번호 구분선 vs 번호 제목 혼동, 완결 문장 오탐 등으로 경계 인식률이 낮음 | 경계 확정을 "접미사(특별약관/보통약관 등) 필수 pending 병합" 구조로 재설계 | **경계 감지(목차 정답 대비): 30327 정밀도 97%/재현율 95%, 프리미엄2604 90%/92%**. 이전엔 프리미엄 특약 144개 중 92개만 감지 |
| `96badb5` feat(rechunk) | 밀집 조판(한 페이지에 특약 2~3개)에서 조각 중간 조 헤딩을 못 잡아 앞 조에 흡수됨. 페이지 단위 라벨 전환이라 페이지 전체가 마지막 특약으로 잘못 칠해짐(DB류 섹션 147개가 뭉침) | 조각 내부 조 분할 추가, 페이지 내 다중 경계를 "라벨이 나타나는 조각"부터 전환하도록 세분화 | **DB 섹션 147→290 분리**(정상 세분화), **조번호갭: 프리미엄2604 25→1**, 조복원율 72→74% |
| `c3b8fd1` fix(chunker) | 정의 조항 안의 "형법 제297조(강간)/제298조(강제추행)/…" 같은 타법령 인용 나열이 조 분할 지점으로 오인됨 | 헤더 매치 사이에 실질 본문이 없는 연속 헤더는 분할 대상에서 제외 | (다음 커밋들의 회귀 게이트로 간접 검증) |
| `ef9f57b` test(eval) | 경계·조 분할을 고칠 때마다 회귀 여부를 눈으로만 확인하던 상태 | **회귀 게이트 구축**: `regress3.py`(3문서 조번호갭/조복원율), `boundary_eval.py`(문서 자체 목차를 정답으로 삼아 경계 정밀도/재현율 측정 — 클로드 참조본은 자체 파편화가 있어 정답으로 부적합했음이 확인돼 채택 안 함) | 이후 모든 경계/조 분할 수정이 이 게이트를 통과 조건으로 삼음 |
| `5c919c6` fix(pdf_parser) | 표 bbox와 조금이라도 겹치는 텍스트 **블록 전체**를 버려서, 표에 인접한 조 헤딩 줄이 유실됨(프리미엄2604 p332 제9조) | 텍스트 제외 판정을 블록 단위→**줄 중심점이 표 bbox 안에 있을 때만** 제외로 정밀화 | **3문서 게이트: 조번호갭 전부 0** |
| `df20589` test(eval) | 20개 약관 전체에 대한 배치 검증 도구 부재 | `batch_gaps.py`(전체 PDF 일괄 조번호갭), `make_chunks_jsonl.py`(재청킹 v4 생성기), `db_restore_analysis.py`(조 라벨 None의 정당/비정당 분리) | **20약관 배치: 총 갭 605→127**, DB빅히트 보정 복원율 98% |
| `22e9d34` feat(eval) | 검색 품질 비교 체계 부재 | `retrieval_eval.py`에 v3(boilerplate 제외)/claude(VLM 병합)/v4(경계 재설계) 트랙 추가, RERANK 환경변수, 임베딩 장문 크래시 대응(1800자 절단+폴백) | **47문항 rerank 기준 v2/v3 R@5 0.851, v4 0.809** — 이 시점 v4가 v3를 못 넘어섬(→ 후일 v5/v6에서 해결) |

---

## 2. 08-09 — 특약 경계 오류 수정 라운드 (탐지→회귀→재수정의 반복)

08-08에서 만든 회귀 게이트(3문서 + 20약관 배치)를 돌리며 발견된 개별 오류를 하나씩 좁혀나간
하루. 특히 뒤 2건은 **1차 시도가 새 회귀를 내서 실측으로 되돌리고 재수정**한 과정이 그대로 기록돼 있음.

| 커밋 | 문제 | 원인 | 조치 | 근거(실측) |
|---|---|---|---|---|
| `1cf3f69` fix(rechunk) | DB빅히트 보통약관 제1~20조가 전부 미복원 | `base`("{상품명} 보통약관") 라벨은 재구성 문자열이라 원문에 없는데, 조각 단위 라벨 전환(전날 도입)이 원문 포함 검사만 해서 base로는 절대 전환 안 됨 → 제1조가 영영 None → 단조증가 가드가 뒤따르는 모든 조를 "큰 점프"로 보고 연쇄 거부 | base 전환은 "제1조(" 시작 신호로 별도 처리 | (다음 항목과 함께 검증) |
| `e10ba6d` fix(boundaries) | 실손류 문서에서 법령 인용 부록(의료법 제36조 등 수십 페이지)의 조번호가 직전 특약 조로 흡수 | "약관에서 인용된 법·규정"/"특별약관 색인" 뒤 법령 전문을 별도 구획으로 분리하지 않음 | 해당 헤더를 `byeolpyo` 경계로 지정해 뒤 내용을 분리 | **3문서 게이트 갭 전부 0 유지. 20약관 배치 총 갭 127→75**(다이렉트실손 20→0, 삼성실손 29→0, 실손2605 3→0) |
| `453d52c` fix(boundaries) | 간편31/통합간편의 하위그룹 번호 제목("2-1. 갱신형 중증화상환자…")이 접두어를 통째로 잃음 | ① DIVIDER가 순번(`\d+.`)만 인식, 하이픈 번호(`2-1.`)는 continuation 근거로 못 봄 ② continuation 판정이 pending[0]만 검사해 "제2절 보장조항" 같은 장 구분이 먼저 끼면 진짜 제목이 검사를 피해감 | 하이픈 번호 인식 확장 + head 전체 검사로 확장 + 절/관/장/편 구분 헤더를 제목 후보에서 원천 차단(`_CHAPTER_DIV`) | **간편31건강보험2604 갭 30→0, 통합간편건강보험2604 갭 24→0** |
| `04ce1f6` fix(rechunk) | 간편31/통합간편의 "산정특례대상" 정의 조항에서 실제 제4~9조가 국민건강보험법 조번호로 덮어써짐 | "의료급여법 제10조… 동법 시행령 제13조…" 형태에서 앞줄이 법령명으로 끝나면, 조사 목록(CITE)에 없는 접속어("동법", "및") 뒤 인용도 단조증가 가드 범위 안이면 조용히 받아들여짐 | 앞줄이 법령명으로 끝나면 분할 금지 | (다음 커밋에서 함께 정량화) |
| `3e3bea7` fix(rechunk) | 30324 "자원봉사활동중 성폭력범죄위로금 특별약관" 갭 9 | 형법 제32장 조문 나열("제297조/제298조/…")이 CITE 조사 검사를 피해 cur_art를 인용 조번호로 덮어씀. **1차 시도**(줄 전체가 헤딩만이면 무조건 스킵)는 "제3조(정의)" 직후 "제4조(청구서류)"처럼 본문이 표로 밀려 헤딩만 있는 **정상 조항**까지 걸러 DB빅히트 "출산비용 특별약관"을 0→갭4로 깨뜨림(회귀 발생) | 진짜 인용 목록(5개+ 연쇄)과 정상 조항(1~2개)의 차이를 이용해, **헤딩-only 줄 3개 이상 연쇄일 때만** 인용으로 판정 | **20약관 배치: 30324 갭10→1, 나머지 19문서 전부 무변화(회귀 없음 확인). 합계 21→12** |
| `801bcea` fix(rechunk) | 치아이목구비2601 "치아치료보장(임플란트/치조골이식 무제한)" 갭6 | 컬럼 폭에 밀려 조사가 다음 줄로 넘어가는 인용("제6조(…)\n에서 정한…")을 CITE가 못 잡아 조 번호 오염. **1차 시도**(조사 뒤 공백/쉼표 경계 lookahead 추가)는 확인 안 된 가상의 위험을 막으려다 "제3항에서"처럼 조사가 붙어 나오는 정상 한국어 패턴까지 걸러 3개 문서(간편31/간편한정기/통합간편)에 새 회귀 | CITE 조사 검사가 줄바꿈을 건너뛰도록 허용, 근거 없이 추가했던 lookahead는 실측 후 제거 | **치아이목구비 갭6→0, 나머지 19문서 전부 무변화(회귀 없음 확인)** |

---

## 3. 08-10 — 조사 기반 검색 품질 개선 사이클

이날은 방향이 다르다: 사용자 요청으로 **외부 연구자료를 조사**하고(InsQABench, PolicyBot,
OHRBench 등 7건), 그걸 코드와 대조해 갭을 찾고, 우선순위를 정해 실행했다. 모든 항목이
"자료 근거 → 우리 코드 실측 확인 → 수정 → eval 게이트" 순서를 거쳤다.

### 3-1. 조사·문서화

| 커밋 | 내용 |
|---|---|
| `a5b7400` docs(eval) | `RESEARCH_REPORT.md`(외부자료 7건 핵심 정리), `GAP_ANALYSIS.md`(코드 대조+백로그), `WORK_PLAN.md`(아토믹 커밋 계획) 작성. exclusion 약점의 원인이 청킹 입도라는 것과 프로덕션 임베딩 버그 2건을 이 단계에서 코드 실측으로 확정 |

### 3-2. 프로덕션 버그 수정 (조사에서 발견 → 코드로 확정)

| 커밋 | 문제 | 근거 | 조치 |
|---|---|---|---|
| `1697416` fix(search) | 프로덕션 `db/search.py`가 질의를 프리픽스 없이 임베딩. 문서는 원문 그대로 색인되므로 qwen3-embedding의 비대칭 검색 사용법과 eval 측정 조건 모두와 어긋남 → **프로덕션 벡터 recall이 eval 수치보다 저평가될 개연성** | `retrieval_eval.py:178`은 프리픽스 사용, `db/search.py:160`은 미사용 — 코드 직접 대조로 확인 | `embedder.py`에 `QUERY_INSTRUCT`+`embed_query()` 추가, 검색 경로가 사용하도록 정렬. 문서 벡터 불변이라 재인덱싱 불필요 |
| `167c09a` refactor(eval) | eval에 중복 상수 존재 | — | `QUERY_INSTRUCT`를 embedder에서 단일 출처로 import |
| `8c2042a` fix(embedder) | 프로덕션 기본 임베딩 태그 `qwen3:embedding`이 ollama에 **존재하지 않는 태그** | `ollama list` 직접 확인 — 실제 태그는 `qwen3-embedding:0.6b`(eval 기본값과 동일) | 기본값을 실재 태그로 정렬, README·CONTEXT 문서 동기화 |

### 3-3. exclusion(면책) 약점 해소 — 호 단위 분할 (v5)

| 커밋 | 근거 | 조치 | 실측 |
|---|---|---|---|
| `547da94` feat(rechunk) | `GAP_ANALYSIS.md` §2: claude 청킹만 exclusion R@5 0.86인 이유를 실패 케이스 역추적으로 확정 — **호 단위 세분할이 유일 원인**(긴 면책조를 통짜로 두면 희소어 "스카이다이빙"·"오토바이" 신호가 희석돼 BM25 후보풀에 못 듦) | 조 토큰 ≥400 && 호/목 항목 ≥4인 조만 항목 단위로 분할, 도입부를 각 조각에 승계, `RECHUNK_ENUM_SPLIT=0`으로 이전 동작 재현 가능 | — |
| `3a08b70`/`d6280d9` chore/docs(eval) | v5(v3+호분할, boilerplate 제외, 1250청크) 등록·평가 | **게이트 통과**: rerank 기준 전체 R@5 0.851→**0.872**, MRR 0.764→**0.770**, **exclusion lenient R@5 0.57→0.71**. 메커니즘 검증: BM25 R@5도 0.702→0.745 상승(예측한 term-density 메커니즘 확인). 단 embed 단독은 0.851→0.809로 하락 |
| `a6e8aef` feat(search) | v5는 rerank 전제로만 v3를 이김(embed 단독 하락) | `db/search.py`의 rerank 기본값을 `SEARCH_RERANK` 환경변수로 제어(시그니처 호환 유지) | — |

### 3-4. 무결성 측정 사각지대 보완

| 커밋 | 근거 | 조치 |
|---|---|---|
| `0d704e5` feat(eval) | `GAP_ANALYSIS.md` §3: OHRBench 대조 — 기존 coverage 지표는 `norm()`이 파이프·공백을 지워 **표 구조 붕괴에 무감각**함을 확인 | `table_ragged`(표 청크 행별 열 개수 불일치율), `table_page_recall`(원문 표 페이지 대비 커버율), `art_nonmono`/`cite_runs`(조번호 역행·비정상 점프 — 타법령 인용 오인 감시), `--vlm` 모드(Surya vs pymupdf 승률) 4종 지표 추가 |

### 3-5. 새 지표가 발견한 것 → 즉시 대응 (경계 복구, v6)

| 커밋 | 근거 | 조치 | 실측 |
|---|---|---|---|
| `1921cbb` feat(rechunk) | 새로 만든 `art_nonmono` 지표로 17문서 배치를 돌려본 결과 **79건** 발견: boundaries가 점수 미달로 놓친 특약 경계에서 새 특약의 조가 이전 특약 section에 계속 붙는 문제(조번호가 3→1처럼 리셋되는데 섹션 라벨은 안 바뀜) | `clean()`에서 특약 진행 중 제1조 리셋을 감지하면 직전 조각 끝의 특약 제목 줄(`TITLE_SUFFIX`)을 새 섹션으로 승격, 다음 진짜 경계까지 유지. 제목 줄을 못 찾으면 기존 동작 그대로(보수적 — 오탐 방지) | **30327 nonmono 4→3, 30324 17→15, gaps·청크수 회귀 없음** |
| `fbb7959`/`e36307b` chore/docs(eval) | v6(v5+경계복구) 등록·평가 | — | **rerank 기준 v5→v6: R@1 0.702→0.723, R@5 0.872→0.894, MRR 0.770→0.791.** coverage 유형 0.89→1.00(복구된 섹션 라벨이 정답 매칭 회복), exclusion 0.71 유지. **lenient R@5 0.894로 claude 청킹(VLM 병합)과 동률, strict는 0.894>0.872로 추월** |

### 3-6. 검증했으나 비채택 (실측으로 배제 — 기록 가치 있음)

| 항목 | 가설 | 실측 결과 | 판정 |
|---|---|---|---|
| VLM(Surya) 표 붕괴 대응 | table_ragged 최악 문서(치아보험 21.4%)에 VLM 적용하면 개선될 것 | 20.8%로 사실상 무개선(35분 비용) — 원인이 추출 품질이 아니라 **병합 셀→markdown 평탄화 단계**의 손실이라 추출기 교체로는 안 풀림 | **비채택.** 후속 후보: 병합 셀 표만 HTML 유지하는 하이브리드 |
| LoyJoy식 "상호참조" exclusion 원인 가설 | 면책조항이 보장조항과 떨어져 있어 문맥 부족이 실패 원인일 것 | exclusion 실패 7건 전수 역추적 결과 **원인 0건** — 한국 약관 면책조는 자기완결적 리스트라 해당 없음. 실제 원인은 청킹 입도(위 3-3) | **비채택.** 외부 프레임을 자사 데이터로 검증 후 폐기한 사례 |

---

## 요약 — 3일간의 변화

| 지표 | 08-08 이전 | 08-10 최종(v6) |
|---|---|---|
| 20약관 배치 조번호갭 총합 | 605 | 12 (08-09 종료 시점) |
| art_nonmono(특약 경계 누락, 17문서) | 79 | 개선(30327/30324 검증, 전체 재측정은 다음 배치 예정) |
| 검색 R@5 (rerank, 47문항) | 0.851(v3) | **0.894**(v6, claude VLM 청킹 추월) |
| exclusion lenient R@5 | 0.57 | **0.71** |
| 프로덕션 검색 버그 | 질의 프리픽스 누락, 임베딩 태그 오설정 (미인지) | 수정 완료 |

모든 수치는 각 커밋에 기록된 회귀 게이트(3문서 상시 게이트, 20약관 배치, 47문항 retrieval_eval)
실행 결과이며, 별도로 재현하려면 `eval/regress3.py`, `eval/batch_gaps.py`,
`eval/batch_integrity.py`, `eval/retrieval_eval.py --run`을 순서대로 실행하면 된다.
