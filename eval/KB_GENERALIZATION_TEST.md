# 타사 약관 일반화 테스트 — KB손해보험

> 대상: `in/KB_금쪽같은자녀보험Plus_26.04.pdf` (1250p, 제121조, 8파트 병합 28MB)
> 노션 출처: [KB] KB 금쪽같은 자녀보험Plus(무배당)(26.04)_1종_세만기 (시행 2026-05-01, 버전 25159)
> 실행일: 2026-08-10 / 코드: v6 (호 단위 분할 + 특약 경계 복구)

---

## 1. 목표 — 무엇을 확인하려는가

**한 문장: "우리 청킹 파이프라인이 메리츠화재 문서에만 맞춰진 것인지, 실제로 한국 보험약관 일반에 통하는지"를 처음으로 검증한다.**

구체적으로 세 가지를 잰다:

| 확인 항목 | 측정 지표 | 의미 |
|---|---|---|
| ① 경계 검출이 타사 조판에서도 되는가 | 섹션 수, `art_nonmono` | 특약 제목 인식 = 파이프라인의 최상단 전제 |
| ② 조 구조 복원이 되는가 | `gaps`(조 결번), 조번호 복원율 | 조 단위 청킹의 성립 여부 |
| ③ 본문·표가 유실 없이 담기는가 | `coverage`, `table_ragged`, `table_page_recall` | 청크 자체의 신뢰도 |

---

## 2. 왜 필요했나 — 문제의식

### 2-1. 지금까지의 검증은 전부 한 보험사였다

3일간(8/8~8/10) 집중 개선으로 검색 R@5를 0.851 → 0.894까지 올렸고, 20약관 배치 조번호 오류를 605 → 12로 줄였다. **그런데 그 20개 약관의 실체를 보면:**

- `batch_integrity`로 처리한 17문서 = **전부 메리츠화재** (프리미엄/간편/올바른/치아/실손/단체안심 계열)
- 상시 게이트 3문서 = 메리츠 30327 + 메리츠 프리미엄2604 + DB빅히트
- 검색 평가 47문항 = **메리츠 단체안심생활보험(30327) 단일 문서**

즉 **"보험약관 청킹 파이프라인"이라 부르고 있지만 실증된 범위는 사실상 메리츠화재 한 곳**이다.

### 2-2. 그동안의 수정이 "일반 규칙"인지 "메리츠 맞춤"인지 구분이 안 됐다

국면 C에서 고친 것들을 다시 보면 대부분 **특정 문서에서 발견 → 규칙 추가**의 형태였다:

- 하이픈 번호 제목(`2-1.`) → 간편31·통합간편에서 발견
- 법령 인용 부록 분리 → 실손류에서 발견
- 헤딩-only 3개 연쇄 = 인용 → 30324에서 발견
- 줄바꿈 조사 인용 → 치아이목구비에서 발견

각각은 20문서 배치로 "다른 문서에 회귀 없음"을 확인했지만, **그 20문서가 전부 같은 보험사면 "회귀 없음"의 증명력이 약하다.** 메리츠 조판 관습에 맞춘 규칙 20개를 쌓아올린 것일 수도 있다.

### 2-3. 실제 서비스는 여러 보험사 약관을 다룬다

노션 DB(`보험 약관 파일`)를 조회한 결과, 수집돼 있으나 **한 번도 처리하지 않은 문서가 대량으로 존재**한다:

- **KB손해보험** — 자녀보험, 5.10.10, Yes!365, 닥터플러스, 간편건강 계열 수십 건 → **처리 0건**
- **DB손해보험 프로미라이프** — 간편건강 계열 다수 → 처리 0건 (기존 "DB빅히트"는 별개 상품)
- 메리츠 미처리분 — 알파Plus, 케어프리, 내Mom같은 어린이보험 등

서비스가 이 문서들을 받게 되면 지금 파이프라인이 어떻게 동작하는지 **아무도 모르는 상태**다. 배포 전에 알아야 한다.

### 2-4. 실패해도 이득이다

이 테스트는 "잘 되면 좋고 안 되면 손해"가 아니다:
- **잘 되면** → 파이프라인의 일반성이 실증되고, 신규 보험사 온보딩 비용이 낮다는 근거가 생긴다
- **안 되면** → **다음 개선 사이클의 타깃이 데이터로 확정된다.** 지금은 남은 백로그(부록 5의 12건)가 전부 메리츠 문서 기반 추정인데, 타사 실패 유형이 나오면 우선순위가 바뀔 수 있다

---

## 3. 사전 가설 — 어디서 깨질 것 같은가

문서를 열어 목차를 확인한 결과, KB는 메리츠와 다른 조판 관습을 쓴다:

```
「출생전 자녀가입 제도성 특별약관」 부가 특별약관
245. 비급여(전액본인부담 포함) 암 특정주요치료비Plus(상급종합병원)(감액없음)
 245-1. 비급여(전액본인부담 포함) 암 특정주요치료비Plus(상급종합병원)(감액없음)【갱신계약】
247. 암(유사암제외) 주요치료(연간2,3,4회이상) 생활비(상급종합병원)(감액없음)
```

**가설 H1 (가장 위험) — 특약 제목에 접미사가 없어 경계 검출이 대량 실패한다.**
우리 `boundaries.TITLE_SUFFIX`는 `(특별약관|보통약관|특약|담보)`로 끝나는 줄만 제목 후보로 인정한다(`_W_SUFFIX=1.6`은 점수제에서 가장 큰 가중치). 그런데 KB 특약명은 `…(감액없음)`, `…【갱신계약】`처럼 **접미사 없이 끝나는 형태**가 다수다. 이러면 경계 후보 자체가 안 잡혀 섹션이 통째로 뭉칠 수 있다.
→ 예상 증상: 섹션 수가 비정상적으로 적음, `art_nonmono` 급증(새 특약의 제1조가 이전 섹션에 계속 붙음)

**가설 H2 — 하이픈 번호(`245-1.`)와 세 자리 번호는 이미 대응돼 있을 것.**
`453d52c`에서 DIVIDER에 하이픈 번호 인식을 넣었으므로 이 부분은 통과 예상. 다만 그때는 `2-1.` 형태였고 여기는 `245-1.`(세 자리)이라 정규식 범위 확인 필요.

**가설 H3 — `【갱신계약】` 대괄호는 조 제목용 패턴과 충돌 가능.**
`_ART_TITLE`이 `【】`를 조 제목 괄호로 인정하는데, 특약명 꼬리의 `【갱신계약】`이 조 헤딩으로 오인될 여지가 있다.

**가설 H4 — 1250페이지·121조 규모에서 처리 시간·메모리가 실용적인가.**
기존 최대는 337p(30327). 3.7배 규모에서 파이프라인이 완주하는지 자체가 확인 대상.

---

## 4. 판정 기준

메리츠 17문서 평균을 베이스라인으로 삼되, **타사 첫 시도임을 감안해 절대 기준이 아닌 "어느 축이 무너졌나"를 본다.**

| 지표 | 메리츠 평균 | 양호 | 경고 | 실패 |
|---|---|---|---|---|
| `coverage` | 0.830 | ≥0.80 | 0.70~0.80 | <0.70 |
| `gaps`(조 결번) | 0~5 | ≤5 | 6~20 | >20 |
| `art_nonmono` | 4.6/문서 | ≤10 | 11~30 | >30 (= H1 적중) |
| `table_ragged` | 8.1% | ≤10% | 10~20% | >20% |
| `table_page_recall` | 0.977 | ≥0.95 | 0.90~0.95 | <0.90 |
| 섹션 수 | — | 특약 수와 유사 | — | 극단적으로 적음(= H1 적중) |

**H1이 적중하면**(섹션 수 붕괴 + art_nonmono 폭증) 이는 버그가 아니라 **설계 전제의 한계**다 — 조치는 "접미사 없는 특약 제목"을 인정하는 신호를 추가하는 것(번호 접두 + 폰트 + 고립도만으로 판정하는 경로). 다만 오탐이 늘 수 있어 메리츠 20문서 회귀 게이트를 반드시 동반해야 한다.

---

## 5. 이 테스트가 답하지 않는 것 (범위 밖)

- **검색 품질** — KB용 골드셋(질문+정답 라벨)이 없으므로 R@k는 못 잰다. 이번엔 **청킹 무결성만** 본다.
- **최적 파라미터** — 임계값 튜닝은 하지 않는다. 현재 v6 코드가 그대로 어떻게 동작하는지만 관찰한다.
- **다른 보험사 일반화** — KB 1건으로 "타사 일반화 성공"이라 말할 수 없다. DB프로미라이프 등 추가 검증이 필요하다.

---

## 6. 실행 절차

```bash
# 1. 노션에서 다운로드 (단건 CLI 모드 — 이번에 추가)
.venv/bin/python eval/notion_download.py <page_id> KB_금쪽같은자녀보험Plus_26.04

# 2. v6 코드로 청킹
.venv/bin/python eval/make_chunks_jsonl.py \
  "in/KB_금쪽같은자녀보험Plus_26.04.pdf" "eval/chunks_KB_자녀보험.jsonl" \
  "KB손해보험" "KB 금쪽같은 자녀보험Plus(26.04) 1종 세만기"

# 3. 무결성 측정
.venv/bin/python eval/integrity_check.py \
  eval/chunks_KB_자녀보험.jsonl in/KB_금쪽같은자녀보험Plus_26.04.pdf
```

## 7. 결과 — H1 적중, 경계 검출 붕괴

### 7-1. 측정치

| 지표 | KB 자녀보험 | 메리츠 평균 | 판정 |
|---|---|---|---|
| 청크 수 | 6,548 | — | (1250p 완주 — **H4 통과**) |
| **섹션 수** | **37** | 130~477 | **실패** ← 특약 수백 개인 상품 |
| **art_nonmono** | **263** | 4.6 | **실패** (기준 >30의 8.8배) |
| 조복원율 | 0.594 | ~0.72 | 경고 |
| coverage | 0.787 | 0.830 | 경고 |
| gaps(조 결번) | **0** | 0~5 | **해석 주의 — 아래 참조** |
| table_ragged | 19.0% | 8.1% | 경고 (치아·실손류와 동급) |
| table_page_recall | 0.958 | 0.977 | 양호 |

### 7-2. 무슨 일이 일어났나 — 섹션 라벨의 정체

섹션 37개의 실제 라벨을 열어보니 **정상적인 특약명이 하나도 없다.** 전부 문장 조각이 라벨로 승격된 것:

| 청크 수 | 섹션 라벨(실제) |
|---|---|
| 1,619 | `제2항에도 불구하고 "제도성 특별약관 - 보장특약 자동갱신(추가납입형) 특별약관` |
| 825 | `가. 초회보험료자동납입 추가특별약관` |
| 547 | `약관에서 인용된 법·규정` |
| 432 | `별표1 (장해분류표)에서 정한 기준에 따른 장해상태를` |
| 398 | `해약환급금 산출방법서"에서 정하는 바에 따라 피보험자의 사망 당시 이 특별약관` |
| 260 | `별표126 (정신질환Ⅲ 분류표)에서 정한 질병을 말합니다.` |

**진단:** KB 특약 제목은 `245. 비급여… 암 특정주요치료비Plus(상급종합병원)(감액없음)`처럼 **"특별약관" 접미사 없이** 끝난다. 우리 `TITLE_SUFFIX`(가중치 1.6, 점수제 최대)가 이걸 후보로 안 올린다. 대신 **본문 문장 중간에 우연히 "특별약관"이라는 단어가 포함된 줄**("…제2항에도 불구하고 …특별약관")이 접미사 점수를 받아 경계로 오인됐다. 즉 **진짜 제목은 놓치고 가짜 제목만 잡은 완전 역전 상태.**

최대 섹션 하나에 청크 1,619개(전체의 25%)가 뭉쳐 있고, 그 안에서 조번호가 **774개 시퀀스 중 116회 역행**(1→7→1→2→1→…)한다. 수십 개 특약이 한 라벨 아래 압축된 것.

### 7-3. ⚠️ 예상 못 한 발견 — gaps=0은 좋은 신호가 아니었다

**`gaps`(조 결번)가 0으로 완벽하게 나왔다. 그런데 이건 최상의 결과가 아니라 붕괴의 증거다.**

- `gaps`는 *섹션 내* 조번호 min~max 사이의 빠진 번호를 센다.
- 수십 개 특약이 한 섹션으로 뭉치면 1~121조가 전부 들어차므로 **결번이 생길 수 없다** → 자동으로 0.
- 반대로 `art_nonmono`(역행·점프)는 뭉친 만큼 폭증한다 → 263.

**교훈: 경계가 정상일 때만 `gaps`가 유효한 지표다.** 경계가 무너진 문서에서 gaps를 보면 "완벽하다"는 정반대 결론이 나온다. 두 지표는 항상 함께 읽어야 하며, **`gaps=0 && art_nonmono 폭증`은 "경계 붕괴"의 시그니처**로 등록해야 한다.
(부록 5 #21 "지표는 설계 의도 밖의 것을 잡는다"의 후속 사례 — 이번엔 지표가 *잘못된 안심*을 줄 수 있다는 반대 방향의 교훈.)

### 7-4. 가설 채점

| 가설 | 결과 |
|---|---|
| **H1** 접미사 부재로 경계 검출 실패 | ✅ **적중** — 섹션 37개, nonmono 263, 문장 조각 라벨 |
| **H2** 하이픈 번호(`245-1.`)는 대응됨 | 판정 보류 — H1이 상위에서 막혀 검증 불가 |
| **H3** `【갱신계약】`이 조 헤딩과 충돌 | 판정 보류 — 동일 |
| **H4** 1250p 대규모 완주 | ✅ **통과** — 완주, 표 recall 0.958 |

### 7-5. 결론 — ⚠️ 이 결론은 §8에서 정정됨

*(초기 결론, KB 1건만 보고 내린 것)* "우리 파이프라인은 메리츠화재 조판 관습에 의존한다"가 실증됐다. 3일간의 개선(R@5 0.894)은 유효하지만, 그 유효성의 범위는 접미사로 특약 제목이 끝나는 문서로 한정된다.

**→ 이 일반화는 틀렸다. 보험사 5곳을 추가 측정한 결과는 §8 참조.**

### 7-6. 후속 조치 (다음 사이클 최우선)

1. **접미사 없는 특약 제목 인식 경로 추가** (P0으로 승격)
   - 현재: 접미사(1.6) + 폰트(1.4) + 굵기(1.0) + 고립도(0.8) + 짧은 길이(0.6), 임계 2.4
   - 제안: **번호 접두(`\d+(-\d+)?\.`) 신호**를 새 가중치로 추가해, 접미사 없이도 [번호 + 폰트 + 고립도]만으로 임계 도달 가능하게
   - 필수 동반: 메리츠 20문서 회귀 게이트(현 규칙이 잡던 것을 놓치지 않는지) + 오탐 억제(문장 중간 "특별약관" 포함 줄 차단)
2. **문장 조각 라벨 방어** — 라벨 후보가 조사/서술어로 끝나거나(`…를`, `…합니다.`) 인용 접속(`제N항에도 불구하고`)으로 시작하면 제목에서 배제. 이건 KB뿐 아니라 메리츠 잔존 nonmono에도 효과 예상(부록 5 #36의 "문장 조각 라벨" 유형과 동일).
3. **`gaps=0 && art_nonmono 폭증` 경보 규칙**을 `batch_integrity`에 추가 — 경계 붕괴를 자동 감지.
4. **타사 문서를 상시 게이트에 편입** — 현 3문서 게이트(메리츠 2 + DB빅히트 1)에 KB 1건 추가. 그래야 "일반 규칙 vs 메리츠 맞춤"이 매 수정마다 구분된다.
5. DB프로미라이프 1건 추가 검증 — KB 1건으로 "타사 일반화 실패"를 일반화하지 않기 위해.

---

# 8. 보험사 6곳 확대 측정 — 초기 결론 정정

KB 1건으로 "타사 일반화 실패"라 결론내는 것을 문서 §5에서 스스로 경계했었다. 실제로
노션에서 보험사별 대표 약관을 받아 6건을 측정한 결과, **그 경계가 옳았다 — 초기 결론은 틀렸다.**

## 8-1. 결과 (`eval/multi_insurer_test.py`, 최종 코드)

| 보험사 | 청크 | 섹션 | 조갭 | nonmono | coverage | ragged | 표recall | 판정 |
|---|---|---|---|---|---|---|---|---|
| 메리츠화재 (30327) | 1,837 | 151 | 0 | **3** | 0.799 | 0.000 | 0.975 | ok |
| **KB손해보험** | 6,352 | **7** | 0 | **473** | 0.752 | 0.188 | 0.970 | **⚠️ 붕괴** |
| 현대해상 | 12,019 | 358 | 2 | 154 | 0.795 | 0.053 | 0.992 | ok |
| 삼성화재 | 9,724 | 267 | 0 | 99 | 0.839 | 0.053 | 0.981 | ok |
| DB손해보험(프로미라이프) | 3,212 | 48 | 0 | **0** | 0.830 | 0.200 | 0.844 | ok |
| DB손해보험(구 동부, 빅히트) | 3,749 | 329 | 0 | 26 | 0.871 | 0.152 | 1.000 | ok |

**정상 5 / 붕괴 1.**

## 8-2. 정정된 결론

1. **파이프라인은 메리츠 전용이 아니다.** 현대해상(358섹션)·삼성화재(267섹션)·DB(48/329섹션)
   모두 경계 검출이 정상 동작한다. 커버리지·표 품질도 메리츠와 동급이거나 더 낫다
   (삼성 0.839, DB빅히트 0.871 > 메리츠 0.799).
2. **KB가 예외 케이스다.** 특약명이 접미사(`특별약관`) 없이 `…(감액없음)`으로 끝나는
   조판은 6곳 중 KB에서만 관측됐다. 즉 "설계 전제의 한계"는 맞지만 **적용 범위가
   훨씬 좁다** — 전면 재설계가 아니라 KB류 대응이 필요한 것.
3. **경계 품질은 이분법이 아니라 스펙트럼이다.** `art_nonmono` 기준:
   DB프로미 0 < 메리츠 3 < DB빅히트 26 < 삼성 99 < 현대 154 << KB 473.
   **정상 판정 보험사에서도 놓치는 경계가 상당하다**(현대 154, 삼성 99). 붕괴는 아니지만
   개선 여지가 명확하며, 이건 조번호 리셋 기반 분할이 직접 겨냥하는 부분이다.
4. **표 품질은 보험사가 아니라 상품 유형의 문제다.** ragged가 높은 쪽은
   DB프로미(간병 0.200)·KB(자녀 0.188)·DB빅히트(0.152)인데, 메리츠에서도 치아·실손류가
   0.19~0.21이었다. 병합 셀이 많은 표를 쓰는 상품이 공통 원인 — 보험사와 무관.
5. **새 발견: DB프로미라이프 표recall 0.844.** 6건 중 유일하게 0.9 미만으로,
   원문 표 페이지의 15.6%가 청크에 안 담겼다. 별도 조사 대상.

## 8-3. 실행한 조치 (이 세션)

경계 오탐(문장 조각이 라벨로 승격)을 차단하는 가드 5종을 `boundaries.is_title_like()`로
통합하고, KB 붕괴를 자동 감지하는 지표를 추가했다.

| 가드 | 잡는 것 | 근거 |
|---|---|---|
| `_DANGLING_PARTICLE` | 조사로 끝남 (`…장해상태를`) | KB 표 셀 인용 |
| `_CITE_LEAD` | 인용 접속 시작 (`제2항에도 불구하고 …`) | KB 1,619청크 흡수 |
| `_SENTENCE_MID` | 중간 문장 구조 (`…에서 정하는`) | KB 다수 |
| `_BULLET_ENUM` | 글머리표 열거 (`∙ 제1호 : …`) | KB 3,149청크 흡수 |
| `_MAX_TITLE_LEN=55` | 길이 상한 | 실측: 메리츠 섹션 최대 45자·중앙 17자 |

- `rechunk._rescue_title`(v6에서 도입)이 이 검증을 안 해 KB에서 쓰레기 라벨을 양산하던
  버그도 수정 — `is_title_like`를 태움.
- `batch_integrity`에 `boundary_collapse`(gaps==0 && nonmono > max(30, 섹션수)) 추가.
  **KB에서 실제로 발화**해 자동 감지가 작동함을 확인.
- `regress3.py` 상시 게이트에 KB 편입(`REGRESS_FAST=1`로 스킵 가능).

**메리츠 회귀 없음**: 30327을 코드 변경마다 4회 재생성해 대조 — 청크 1,250 / 섹션 148 전부 불변.

## 8-4. 이 과정에서 얻은 교훈

- **게이트가 못 잡는 회귀가 있다.** `_SENTENCE_MID`에 넣었던 `따른|의한`이 정상 별표 제목
  (`기상현상에 따른 피해 분류표`)을 지웠는데, 별표는 조번호가 없어 **조갭 지표로는 안 잡혔다.**
  청크셋 섹션 집합 diff를 떠야만 발견됐다 → 경계 규칙 변경 시 diff를 게이트에 포함해야 한다.
- **정밀도 수정만으론 붕괴가 안 풀린다.** KB에서 쓰레기 라벨 8개(~2,000청크)를 제거하자
  내용이 더 큰 버킷으로 뭉쳐 nonmono가 263→473으로 **악화**됐다. 잘못된 경계를 없애도
  올바른 경계를 만들지 못하면 순효과가 없다 → 구조적 해법(조번호 리셋 기반 분할)이 필요.
- **1건으로 일반화하지 말 것.** §5에 "KB 1건으로 타사 일반화 실패라 말할 수 없다"고
  적어둔 덕에 확대 측정으로 이어졌고, 실제로 초기 결론이 뒤집혔다.

## 8-5. 다음 단계 (우선순위 갱신)

1. **조번호 리셋 기반 분할** — 경계 위치(리셋)와 이름(제목)을 분리. 이름을 못 찾아도
   분할은 되게. 현대 154·삼성 99·KB 473을 동시에 겨냥하는 유일한 구조적 해법. **최우선.**
2. **목차 앵커 검출** — 문서 자체 목차에서 특약명을 수확해 매칭. 보험사 무관하며,
   목차 대비 검출률이 **문서별 자가 검증 점수**가 된다(사람 검수 없이 품질 판정).
   `boundary_eval.py`의 목차 추출을 평가용 → 검출용으로 승격.
3. DB프로미라이프 표recall 0.844 조사.
4. 보험사별 대표 문서를 상시 게이트로 유지(`multi_insurer_test.py`), 신규 보험사 추가 시 1건씩 편입.
