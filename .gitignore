.venv/
__pycache__/
*.pyc
*.pyo
# 배포 시크릿 — 호스트의 .env는 커밋 금지(deploy/.env.example 템플릿만 커밋)
.env
# CI/CD — 처리 대상 데이터
deploy/data/
*.pdf
*.json
!eval/eval_set.example.jsonl
dry_run_out/
# 실행 이력(runlog) 로컬 폴백 — 운영에선 호스트 볼륨 /data/state에 쌓인다
.state/
*.egg-info/
dist/
.pytest_cache/
# eval 산출물 (파생 데이터 — 골든셋만 커밋)
eval/*.jsonl
!eval/goldenset.jsonl
!eval/questions_30327.jsonl
# qgen.py 산출 질문셋은 온도 0.8 생성이라 재현 불가 — 골든셋으로 커밋
!eval/questions_gen_*.jsonl
eval/*.log
eval/*.csv
